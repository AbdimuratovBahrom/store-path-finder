



<!DOCTYPE html>
<html lang="{{ locale }}">




<head>
  <meta charset="UTF-8">
  <title>{{ _("Store Path Finder") }}</title>
  <link rel="stylesheet" href="/static/style.css">
</head>

<body>
  <div class="header">
    <div class="language-selector">
      <label for="languageSelect">{{ _(" ") }} </label>
      <select id="languageSelect" onchange="changeLanguage(this.value)">
        <option value="ru" {% if locale=='ru' %}selected{% endif %}>RU</option>
        <option value="uz_Latn" {% if locale=='uz_Latn' %}selected{% endif %}>UZ (Lotin)</option>
        <option value="uz_Cyrl" {% if locale=='uz_Cyrl' %}selected{% endif %}>УЗ (Кирилл)</option>

      </select>
    </div>

    <div class="toggle-btn">
      <button type="button" onclick="toggleBlocks()">{{ _('Скрыть блоки') }}</button>
      
      <div id="blocks-container" style="display:block;">
        <!-- здесь твои формы выбора блоков/рядов -->
      </div>
    </div>
  </div>

  <div class="search-form">
    <form action="/search" method="get">
      <input type="text" id="keyword" name="keyword" placeholder="{{ _('Введите номер или название...') }}">
      <button type="submit">🔍</button>
    </form>
  </div>

  <div id="search-results"></div>

  <div class="container">
    <h1>{{ _("Поиск линий электроснабжения магазинов") }}</h1>

    <select id="block" onchange="loadRows()">
      <option value="">{{ _("Выберите блок") }}</option>
      {% for block in blocks %}
      <option value="{{ block }}">{{ block }}</option>
      {% endfor %}
    </select>

    <select id="row" onchange="loadStores()" disabled>
      <option value="">{{ _("Выберите ряд") }}</option>
    </select>

    <select id="store" onchange="getPath()" disabled>
      <option value="">{{ _("Выберите магазин") }}</option>
    </select>

    <div id="pathResult" data-error-rows="{{ _('Ряды не найдены') }}" data-error-stores="{{ _('Магазины не найдены') }}"
      data-error-path="{{ _('Путь не найден') }}">
    </div>
  </div>

  <div class="case">
    <a target="_blank" href="https://abdimuratovbahrom.github.io/tashkent-trade-center/">
      {{ _("Блоки 1,2,3") }}
    </a>
  </div>

  <script src="/static/script.js"></script>
</body>

</html>


блоки , ряд и магазины выподают и выдает результат , но мне бы хотелось бы